# Allow npm to install packages even when peer dependency versions don't exactly match.
# Needed because eslint-plugin-jsx-a11y@6.x peer-dep declaration hasn't caught up to eslint v10.
# Safe to use — this only affects install resolution, not the production build.
legacy-peer-deps=true
